When required to run any `mvn spring-boot:run`, use the `@run-exit-java.bat`
When running `npm run dev`, use the `@run-exit-npm-dev.bat`
When running `npm create vite@latest . -- --template react-ts`, use `echo Y | npm create vite@latest . -- --template react-ts` to skip manual confirmations.